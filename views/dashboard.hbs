<h5>Dashboard</h5>

<h3>Welcome, {{name}}</h3> 

<p>Here are all the rants you've made</p>

{{#if rants}}
    <table class="striped">
        <thead>
            <tr>
                <th>Title</th>
                <th>Date</th>
                <th>Status</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            {{#each rants}}
                <tr>
                    <td><a href="/rants/{{_id}}"></a>{{title}}</td>
                    <td>{{formatDate createdAt 'MMMM Do YYYY, h:mm:ss a'}}</td>
                    <td><span class="dash-status">{{status}}</span></td>
                    <td></td>
                </tr>
            {{/each}}
        </tbody>
    </table>
{{else}}
<p>You currently have no rants, feel free to rant away!</p>
{{/if}}